How to play:
       make and execute (gcc, c++11 needed)

       Keys:
                `[SQ][SQ]` --> Move  (e.g. `e2e4`)
                `cpu`      --> Let engine move
                `analysis  --> Analyze position
                `undo`     --> Takes back last move
                `quit`     --> Quits engine

                Initials for these instructions are recognized

TODO:
          - add quiescence search for horizon effect
          - add time_dependent DEPTH
          - work on evaluation function
          - build lookup table